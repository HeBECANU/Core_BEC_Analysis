function header(vargin)
    % A function that makes nice sub headers in the console
    % Works OK with numeric substitution, but needs a length checking
    % function.
    warning('this function has been migrated to cli_header and will be removed in a future release')
    cli_header(vargin)

end